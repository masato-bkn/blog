<%= form_with(model: article.goods.build, remote: true) do |f| %>
    <%= hidden_field_tag :article_id, article.id %>
    <%= f.button :type => "submit", class: "good-btn" do %>
      <i class="far fa-thumbs-up fa-fw"></i>
    <% end %>
    <%=  article.goods.count %>
  <% end %>
